
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: 'Create Form Template | Admin Dashboard',
    description: 'Create dashboard form templates.'
};

const Page = () => {
    return null;
};

export default Page;
